---
title: Renderers
description: Use one token stream everywhere.
---
# Renderers

Use `CSSHighlightRenderer` in the browser, `renderHTML` for SSR, `renderANSI` for terminals, and `renderJSON` for tooling.
